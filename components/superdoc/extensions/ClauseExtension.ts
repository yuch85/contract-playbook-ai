
// DEPRECATED: Custom clause nodes are no longer used.
// We now use native block attributes (id, risk, status) on standard nodes (paragraph, heading, etc.)
// to ensure full DOCX compatibility and simplified export logic.

export const getClauseExtension = () => {
  return null;
};
